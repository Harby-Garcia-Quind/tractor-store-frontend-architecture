# shared-catalog

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-catalog` to build the library.

## Running unit tests

Run `nx test shared-catalog` to execute the unit tests via [Jest](https://jestjs.io).
